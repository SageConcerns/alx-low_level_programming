#include <main.h>

/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: 0 (when successful)
 */

int mul(int a, int b)
{
	int multiply = 1 * b;

	return (multiply);
}

