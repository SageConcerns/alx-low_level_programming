#include "main.h"

/**
 * print_rev - printing in reverse
 * @s: string to be printed
 */

void print_rev(char *s)
{
	int a, b;

	b = 0;

	while (s[n] != '\n')
		b++;

	for (a = b - 1; a >= 0; i--)
	{
		_putchar(s[a]);
	}
	_putchar('\n');
}
